---
interface Props {
  text: string;
  class?: string;
}

const { text, class: className = '' } = Astro.props;
---

<span class:list={["inline-block uppercase tracking-[0.06em] text-xs font-semibold px-4 py-1.5 rounded-full border border-mc-primary text-mc-primary", className]}>
  {text}
</span>
